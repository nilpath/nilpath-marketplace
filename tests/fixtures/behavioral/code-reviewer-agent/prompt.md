Use the code-reviewer agent to review the code in the src/ directory.

Note: This is a non-interactive run. There is no git repository — if git diff commands fail, review the files in src/ directly using Read and Glob. Treat all user approval steps as already approved and proceed to completion without calling AskUserQuestion.
