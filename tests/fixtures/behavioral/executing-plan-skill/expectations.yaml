# The executing-plan skill must be invoked, delegate to an implementation agent,
# and then run code-reviewer after the task.

required_invocations:
  - type: Skill
    name: executing-plan
  - type: Agent
    subagent_type: "claude-code-tools:review:code-reviewer"

expected_sequence:
  - type: Skill
    name: executing-plan
  - type: Agent
    subagent_type: "claude-code-tools:review:code-reviewer"
