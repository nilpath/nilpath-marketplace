# The researching skill must be invoked and eventually spawn spec-reviewer.

required_invocations:
  - type: Skill
    name: researching
  - type: Agent
    subagent_type: "claude-code-tools:review:spec-reviewer"

expected_sequence:
  - type: Skill
    name: researching
  - type: Agent
    subagent_type: "claude-code-tools:review:spec-reviewer"
